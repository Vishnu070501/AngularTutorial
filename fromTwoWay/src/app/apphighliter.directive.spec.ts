import { ApphighliterDirective } from './apphighliter.directive';

describe('ApphighliterDirective', () => {
  it('should create an instance', () => {
    const directive = new ApphighliterDirective();
    expect(directive).toBeTruthy();
  });
});
